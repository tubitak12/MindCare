name: mindcare_app
description: MindCare - Ruh Hali İyileştirme Uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  
  # Güvenlik ve Şifreleme
  encrypt: ^5.0.3
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3
  
  # Ses
  audioplayers: ^6.1.0
  
  # Yardımcı paketler
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  google_generative_ai: ^0.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/